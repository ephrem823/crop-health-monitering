services:
  - type: web
    name: ethio-crop-backend
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: bash start.sh
    envVars:
      - key: PYTHONUNBUFFERED
        value: 1
      - key: TF_USE_LEGACY_KERAS
        value: 0
